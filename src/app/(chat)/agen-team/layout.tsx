export const metadata = {
  title: "Agen Team Dashboard",
  description: "AI Agentic Workflow Command Center",
};

export default function AgenTeamLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
